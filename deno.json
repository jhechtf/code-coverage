{
  "fmt": {
    "options": {
      "singleQuote": true
    }
  },
  "tasks": {
    "test-coverage": "deno test -A  --coverage=coverage && deno coverage --lcov --output=lcov.info coverage && deno run -A cli.ts"
  }
}
